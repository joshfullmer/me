const config = {
  content: ['./src/**/*.{html,js,svelte,ts}'],

  theme: {
    extend: {
      fontFamily: {
        mono: ['Fira Code', 'monospace']
      }
    }
  },

  plugins: []
};

module.exports = config;
